require("toggleterm").setup {
    size = 20,
    open_mapping = [[<C-\>]],
    direction = 'horizontal',
}

-- require("toggleterm").setup {
--     size = 40,
--     open_mapping = [[<A-\>]],
--     direction = 'vertical',
-- }
